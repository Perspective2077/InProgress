# 🧠 1. DNS vs Firewall (completely different jobs)
DNS (name → IP)
```
example.com → 93.184.216.34
```
Firewall (allow / block traffic)
```
Allow or deny packets to an IP/port
```
👉 They are independent systems